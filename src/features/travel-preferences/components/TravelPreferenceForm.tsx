import type { ChangeEvent } from 'react';
import { companionOptions, paceOptions, travelStyleOptions } from '../preference.constants';
import type { TravelPreferences } from '../../../types/travelPlan';
export interface TravelPreferenceFormProps { preferences: TravelPreferences; date: string; startTime: string; partySize: number; onPreferencesChange: (value: TravelPreferences) => void; onDateChange: (value: string) => void; onStartTimeChange: (value: string) => void; onPartySizeChange: (value: number) => void; }
export function TravelPreferenceForm({ preferences, date, startTime, partySize, onPreferencesChange, onDateChange, onStartTimeChange, onPartySizeChange }: TravelPreferenceFormProps) {
  const update = (key: keyof TravelPreferences) => (event: ChangeEvent<HTMLSelectElement>) => onPreferencesChange({ ...preferences, [key]: event.target.value });
  return <><label>선호 관광방식<select value={preferences.style} onChange={update('style')}>{travelStyleOptions.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label><label>여행 속도<select value={preferences.pace} onChange={update('pace')}>{paceOptions.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label><label>동행 유형<select value={preferences.companion} onChange={update('companion')}>{companionOptions.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label><label>여행 날짜<input type="date" value={date} onChange={(event) => onDateChange(event.target.value)} /></label><label>출발 시각<input type="time" value={startTime} onChange={(event) => onStartTimeChange(event.target.value)} /></label><label>인원수<input type="number" min="1" max="10" value={partySize} onChange={(event) => onPartySizeChange(Number(event.target.value))} /></label></>;
}
